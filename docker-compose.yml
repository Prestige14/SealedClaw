version: '3.8'

services:
  sealedclaw-backend:
    build: .
    container_name: sealedclaw-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
